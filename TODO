- Merge all code into one repo (this one, since it has the github pages)
   - dev-gui is over-engineered, we don't need that
   - -api is currently running in heroku
- Get rid of heroku from the process, make that API on AWS
- Fully remove the dev version
- Implement submit in the API
- Archive other repos
